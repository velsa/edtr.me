To run all non socketio tests:
python -m tests.runtests

To run all tests including socketio:
python -m tests.runtests --socketio=True

If new test module is created, then it must be put in runtests TEST_MODULES
